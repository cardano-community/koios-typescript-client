/**
 * Stake Address
 * @export
 * @interface StakeAddress
 */
export interface StakeAddress {
    /**
     * Cardano staking address (reward account) in bech32 format
     * @type {string}
     * @memberof StakeAddress
     */
    id?: string;
}